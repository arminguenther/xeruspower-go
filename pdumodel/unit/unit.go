// SPDX-License-Identifier: BSD-3-Clause
// Copyright 2026 Raritan Inc. All rights reserved.

// Package unit was generated by IDLC from Unit.idl.
// It is part of module pdumodel.
// The remaining comment is for module pdumodel.
//
// PDU Model
package unit

import (
	"context"

	"github.com/arminguenther/xeruspower-go/v40010/event/userevent"
	"github.com/arminguenther/xeruspower-go/v40010/idl"
)

const ERR_INVALID_PARAM int32 = 1 // Invalid parameters

// Unit interface
type Unit interface {
	idl.Object

	// Retrieve the unit metadata.
	//
	//	@return Unit metadata
	GetMetaData(ctx context.Context) (MetaData, error)

	// Retrieve the unit settings.
	//
	//	@return Unit settings
	GetSettings(ctx context.Context) (Settings, error)

	// Change the unit settings.
	//
	//	@param settings  New unit settings
	//
	//	@return 0 if OK
	//	@return ERR_INVALID_PARAM if any parameters are invalid
	SetSettings(ctx context.Context, settings Settings) (int32, error)

	// Display something distictive to identify the unit.
	//
	//	@param seconds  Number of seconds to display the identify string
	Identify(ctx context.Context, seconds int32) error

	// Mute buzzer, turn of all audible alarms.
	//
	//	@param mute  true to mute buzzer, false for normal mode
	MuteBuzzer(ctx context.Context, mute bool) error

	// Retrieve the current orientation of the display.
	//
	//	@return Display orientation.
	GetDisplayOrientation(ctx context.Context) (Orientation, error)
}

// Display orientation
type Orientation int

const (
	NORMAL  Orientation = iota // Normal orientation
	FLIPPED                    // Upside-down
)

// Unit metadata
type MetaData struct {
	HasOrientationSensor         bool          // true if a tilt sensor is present
	SupportedDisplayOrientations []Orientation // Supported display orientations
}

// Unit settings
type Settings struct {
	BuzzerMuted            bool        // true if the buzzer is muted
	AutoDisplayOrientation bool        // true for automatic orientation control
	DisplayOrientation     Orientation // Manually configured orientation
}

// Event: Unit identification requested
type IdentificationStartedEvent interface {
	userevent.UserEvent
	Duration() int32 // Number of seconds the identification should be displayed
	isIdentificationStartedEvent()
}
