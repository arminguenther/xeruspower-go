// SPDX-License-Identifier: BSD-3-Clause
// Copyright 2026 Raritan Inc. All rights reserved.

// Package accumulatingnumericsensor was generated by IDLC from AccumulatingNumericSensor.idl.
// It is part of module sensors.
// The remaining comment is for module sensors.
//
// Sensors Model
package accumulatingnumericsensor

import (
	"context"
	"time"

	"github.com/arminguenther/xeruspower-go/v40412/event/userevent"
	"github.com/arminguenther/xeruspower-go/v40412/sensors/numericsensor"
)

// A sensor which accumulates numeric readings (e.g. energy counter)
type AccumulatingNumericSensor interface {
	numericsensor.NumericSensor

	// Resets the accumulated value of the sensor
	ResetValue(ctx context.Context) error

	// Unix timestamp (UTC) when the value was last reset
	GetLastResetTime(ctx context.Context) (time.Time, error)
}

// Event: Accumulated value has been reset
type ResetEvent interface {
	userevent.UserEvent
	OldReading() numericsensor.Reading // Value before reset
	NewReading() numericsensor.Reading // Value after reset
	isResetEvent()
}
