// SPDX-License-Identifier: BSD-3-Clause
// Copyright 2026 Raritan Inc. All rights reserved.

// Package wlanlog was generated by IDLC from WlanLog.idl.
// It is part of module logging.
// The remaining comment is for module logging.
//
// Device Logging
package wlanlog

import (
	"context"

	"github.com/arminguenther/xeruspower-go/v40510/idl"
	"github.com/arminguenther/xeruspower-go/v40510/logging/log"
)

// WLAN diagnostic log interface
type WlanLog interface {
	idl.Object

	// Clear the WLAN diagnostic log.
	Clear(ctx context.Context) error

	// Get info about the WLAN diagnostic log.
	//
	//	@return log info
	GetInfo(ctx context.Context) (log.Info, error)

	// Fetch a chunk from the WLAN diagnostic log.
	//
	// For details regarding the behavior of this method refer to the description
	// of the logging module in Log.idl.
	//
	//	@param refId      First log id to fetch
	//	@param count      Number of entries to fetch
	//	@param direction  Range direction
	//
	//	@return log chunk
	GetChunk(ctx context.Context, refId int32, count int32, direction log.RangeDirection) (log.Chunk, error)
}
